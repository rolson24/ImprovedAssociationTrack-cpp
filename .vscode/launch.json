{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ImprAssocTrack Test",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/Debug/ImprAssoctrack_test.exe",
            "args": ["--verbose"], // Add any command-line arguments your test needs here
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/data/YOLOX_ncnn_palace", // Set the current working directory to the build directory
            "environment": [], // Add any necessary environment variables here
            "preLaunchTask": "build" // Ensure this task exists in your tasks.json to build your project
        }
    ]
}
